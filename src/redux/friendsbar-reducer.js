let initialState = {
    friendsbar: [
        { id: 1, name: 'Dennis' },
        { id: 2, name: 'Vadim' },
        { id: 3, name: 'Dima' }
    ]
};

const friendsbarReducer = (state = initialState, action) => {


    return state;
}

export default friendsbarReducer;